name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm format:check

      - run: pnpm lint

      - run: pnpm type-check

      - run: pnpm build

      - run: pnpm test

      - run: pnpm test:e2e

  ci-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm type-check

      - run: pnpm build

      - run: pnpm test

      - name: Install and verify the Windows service
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          $stateDir = "C:\labelhost-ci-state"
          node packages/labelhost/dist/cli.js service install `
            --no-tls `
            --port 18080 `
            --state-dir $stateDir

          $task = Get-ScheduledTask -TaskName "Labelhost Proxy"
          if ([string]$task.Settings.ExecutionTimeLimit -ne "PT0S") {
            throw "Expected zero execution time limit, got $($task.Settings.ExecutionTimeLimit)"
          }
          if ($task.Principal.UserId -notin @("SYSTEM", "S-1-5-18")) {
            throw "Expected SYSTEM principal, got $($task.Principal.UserId)"
          }
          if ($task.Principal.RunLevel -ne "Highest") {
            throw "Expected highest run level, got $($task.Principal.RunLevel)"
          }
          if (-not ($task.Triggers | Where-Object {
            $_.CimClass.CimClassName -eq "MSFT_TaskBootTrigger"
          })) {
            throw "Expected a boot trigger"
          }
          if ($task.Actions.Execute -notmatch "(?i)cmd(\.exe)?$") {
            throw "Expected cmd.exe action, got $($task.Actions.Execute)"
          }

          Export-ScheduledTask -TaskName "Labelhost Proxy" | Set-Content labelhost-task.xml
          if ((Get-Content labelhost-task.xml -Raw) -notmatch "<ExecutionTimeLimit>PT0S</ExecutionTimeLimit>") {
            throw "Exported task XML does not contain PT0S"
          }

          $listening = $false
          for ($attempt = 0; $attempt -lt 20; $attempt++) {
            if (Get-NetTCPConnection -LocalPort 18080 -State Listen -ErrorAction SilentlyContinue) {
              $listening = $true
              break
            }
            Start-Sleep -Milliseconds 500
          }
          if (-not $listening) {
            schtasks /Query /TN "Labelhost Proxy" /FO LIST /V
            throw "Labelhost service did not listen on port 18080"
          }

          node packages/labelhost/dist/cli.js service status | Tee-Object service-status.txt

      - name: Upload Windows service evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: windows-service-evidence
          path: |
            labelhost-task.xml
            service-status.txt

      - name: Uninstall Windows service
        if: always()
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          node packages/labelhost/dist/cli.js service uninstall
